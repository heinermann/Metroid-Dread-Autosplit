#include "Backend.h"
#include "opencv2/opencv.hpp"
#include<qdebug.h>
/*
void Backend::routeToSplitImages(Route route) {
	//A series of pauses for an image looks like this:
	//Check split image -> check for black image -> check for next black image -> check first non-black image
	//Each split image (grabbed from a route segment object) should be converted to this series of 4 images.

	for (RouteSegment segment : route.getSegments()) {
		qDebug() << QString::fromUtf8(segment.getMatchingImagePath());
	}
}*/
